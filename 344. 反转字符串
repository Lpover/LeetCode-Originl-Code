class Solution {
public:
    void reverseString(vector<char>& s) {
        int left=0,right=s.size()-1;
        while(left<right){
            char flag=s[left];
            s[left]=s[right];
            s[right]=flag;
            left++;
            right--;
        }
    }
};
